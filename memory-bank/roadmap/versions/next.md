---
status: planning
target_date:
release_date:
features:
  - conversational-season-drafting
  - season-chat-conversation-loop
  - headless-draft-writes
  - client-styling
  - transcript-turn-grouping
---

# next

Planning backlog for YT-Showrunner.

`transcript-turn-grouping` was added on 2026-08-25 — it carried `version: next` in its own
feature file from creation but was never registered here, so `/bmb:roadmap` under-reported
this version by one feature.

**All five features are `status: completed`.** This version is a candidate for
`/bmb:roadmap version release next` — releasing locks it permanently, so that is a deliberate
call, not bookkeeping. Note that `console-dev-ports` is correctly absent: it was a Level 1
`/bmb:task`, which bypasses the roadmap entirely.
